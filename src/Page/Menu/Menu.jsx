import { Helmet } from "react-helmet-async";

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Food Haven || Menu</title>
            </Helmet>
        </div>
    );
};

export default Menu;